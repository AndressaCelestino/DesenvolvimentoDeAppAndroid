<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EleventhModule" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EleventhModule" parent="Base.Theme.EleventhModule" />

    <style name="Theme.EleventhModule.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.EleventhModule.AppOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="Theme.EleventhModule.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark"/>

</resources>